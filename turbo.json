{
	"$schema": "https://turborepo.org/schema.json",
	"tasks": {
		"lint": {},
		"fix:lint": {},
		"format": {},
		"test": {
			"outputs": ["coverage/**"]
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"migrate": {
			"outputs": ["db/migrations/**"]
		}
	}
}
